# Test and lint only — deliberately NOT installed into the production image.
-r requirements.txt
pip-tools==7.6.1     # regenerates requirements.lock
pytest==9.1.1
httpx2==2.12.0          # FastAPI TestClient (starlette deprecates httpx)
ruff==0.14.6
