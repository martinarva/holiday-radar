# Runtime, pinned. A Docker rebuild must produce the same radar it produced
# yesterday: fast-flights parses scraped Google HTML, so an unpinned minor
# release can change what a "price" is without a line of our code changing.
# Bump deliberately, run the suite, commit the new pin.
pyyaml==6.0.3
fast-flights==3.1.0     # Google Flights query builder + parser
typing_extensions==4.16.0   # fast-flights omits this dependency on py3.13+
fastapi==0.141.1        # API + UI (app/api.py)
uvicorn==0.52.4
