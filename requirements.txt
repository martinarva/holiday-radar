# Runtime, pinned. This file states INTENT; requirements.lock is what the
# image installs — it pins the transitive tree too (starlette, pydantic,
# selectolax, primp, protobuf) with hashes, because fast-flights parses
# scraped Google HTML and a silent bump anywhere under it can change what a
# "price" is without a line of our code changing.
#
# To bump: edit here, then
#   pip-compile --generate-hashes --output-file=requirements.lock requirements.txt
# run the suite, and commit both.
pyyaml==6.0.3
fast-flights==3.1.0     # Google Flights query builder + parser
typing_extensions==4.16.0   # fast-flights omits this dependency on py3.13+
fastapi==0.141.1        # API + UI (app/api.py)
uvicorn==0.52.4
