pyyaml>=6.0
fast-flights            # Google Flights query builder + parser (verify stage); pin at E2
typing_extensions>=4.0  # fast-flights omits this dependency on py3.13+
fastapi>=0.115          # dev API + UI (app/api.py)
uvicorn>=0.30
pytest>=8.0
httpx>=0.27             # FastAPI TestClient
