# Copy to .env. Secrets only — all behaviour lives in config.yaml.

# Travelpayouts Data API token (free account): travelpayouts.com -> Tools -> API.
# Without it the radar runs in keyless mode (Ryanair + Google Flights sampling).
TRAVELPAYOUTS_TOKEN=

# Coming with E3 (Home Assistant / MQTT integration):
# MQTT_HOST=
# MQTT_PORT=1883
# MQTT_USERNAME=
# MQTT_PASSWORD=
